#include <stdio.h>

int main(){
    // Observe abaixo um exemplo de estrutura de seleção SIMPLES (if) onde é dada uma condição. Se for verdadeira, executa o bloco de instrução entre as chaves. Senão a secção de instruções é ignorada

    /*int a, b;
    a = 10;
    b = 4;
    if (a > b) {
        printf("O valor %d é o maior que o valor %d", a, b);
    }
    return 0; */

    // Observe abaixo um exemplo de estrutura de seleção COMPOSTA (else) onde é dada mais de uma condição. Se for verdadeira, executa o bloco de instrução entre as chaves. Senão a primeira secção de instruções é ignorada e acessa o segundo bloco de instruções.

    /*int c, d;
    printf("Digite um valor inteiro: ");
    scanf("%d", &c);
    printf("Digite um valor inteiro: ");
    scanf("%d", &d);
    if (c > d) {
        printf("O valor %d eh maior ou igual ao valor %d", c, d);
    }else{
        printf("O valor %d eh maior ou igual ao valor %d", d, c);
    }*/

    // A seguir, temos uma estrutura de seleção múltipla que permite que várias codições sejam previstas (geralmente usado em menus de sistemas)

    /*int a = 2;
    switch (a) {
        case 3:
            printf("errado");
            break;
        case 23:
            printf("errado");
            break;
        case 8:
            printf("gabarito");
            break;
        default:
            printf("AAAAAAAAAAAAAA");
    }*/
}